# Storyboard.Foundation

`Storyboard.Foundation` contains small, dependency-neutral utilities shared by Storyboard products.

The first release contains `Storyboard.Foundation.Paths`: strict resolution for `%ENVIRONMENT_VARIABLE%` path expressions and deterministic asset-root containment helpers.

This package does not contain DTOs, product models, Designer code, GameEngine code, or runtime export behavior.
